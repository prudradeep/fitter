Compare each proposed socio-demographic profile with the existing profiles.

Treat a profile as duplicate when it has the same meaning, is a close paraphrase, names the same group in another language, or is a narrower/restated version already clearly covered. Do not mark it duplicate when it adds a meaningfully distinct group.

Existing socio-demographic profiles for the selected hazard only:
{existing_context}

Proposed profiles:
{proposed_profiles}

Return ONLY valid JSON with this exact shape:
{{"duplicate": false, "match": "", "reason": "", "duplicates": []}}

When any proposed profile is duplicate, set duplicate to true and include duplicates as an array of objects like {{"profile": "new profile", "match": "existing profile", "reason": "short explanation"}}. Keep each reason under 30 words.
