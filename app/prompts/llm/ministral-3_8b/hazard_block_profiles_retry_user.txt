The response returned {profile_count} profiles, but this hazard has {expected_count} confirmed predictors. Return ONLY valid JSON with exactly {expected_count} objects, one object for each PREDICTOR entry. {checklist}
Do not merge, skip, or add predictors.
Do not include Markdown, code fences, comments, trailing commas, extra keys, or
prose outside the JSON.
