$ErrorActionPreference = "Stop"

throw "Deprecated: the Windows installer no longer installs MySQL, creates databases, seeds data, or starts bundled FastAPI services. Configure the hosted backend separately and use the desktop installer only for frontend, hosted backend URL, and local Ollama/model setup."
